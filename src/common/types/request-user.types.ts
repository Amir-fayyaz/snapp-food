export interface IUser {
  user_id: number;
  role: string;
  mobile: string;
}

export interface ISupplier {
  supplier_id: number;
  role: string;
  mobile: string;
}

export interface IAdmin {
  admin_id: number;
  role: string;
}
